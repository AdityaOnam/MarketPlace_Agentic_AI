.~lock.*#
__pycache__/
*.pyc
.DS_Store

# local-only, not part of the GitHub submission repo
PS.odt
6a8ffdf33590a_round3-handout-updated.pdf
sample-marketplace/
WORKLOG.md

# pre-D-013 / pre-D-025 leftovers moved out of the shipping tree on 2026-09-11 so
# harness/package.py's "manifest lists every skill on disk and no others" gate passes.
# Kept locally for reference only; nothing imports them.
_legacy/

# local evaluation harness (Phase 4) -- never part of the submission zip
harness/

# generated working artifacts handed to the user directly (e.g. the gold-labelling kit
# zip) -- reproducible from harness/ output, not part of the submission zip
dist/
*.zip

# unpacked human-labelling kits (frozen HTML snapshots, ~100 MB) -- regenerated by
# harness/gold_worksheet.py, harness/retest.py --emit, harness/r1_worksheet.py
gold-labelling-kit/
retest-kit/
r1-verification-kit/

# aggregated judge-run artifacts and mock fixtures used during the 2026-09-11 external
# review -- not part of the submission, reproducible from harness/out/ if needed
all_84_reports_combined.json
mock_reports.json

# byte-for-byte duplicate of the top-level docs/ tree, kept locally as a working mirror
# inside the shipping directory. docs/ is the authoritative copy that ships.
brand-ai-readiness-audit/docs/

# backups written next to their files during CSV/label recovery (e.g.
# gold-labels-template.csv.corrupted.bak, *.pre-*.bak). Local safety copies only.
*.bak

# teammate's classifier v2 handoff bundle -- the six relevant files were merged into
# the tree in Phase 7; the bundle folder plus its handoff note stay local for reference
# and get deleted by hand later.
classifier_v2_handoff/
HANDOFF-CLASSIFIER.md
