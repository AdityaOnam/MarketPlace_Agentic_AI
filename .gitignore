.~lock.*#
__pycache__/
*.pyc
.DS_Store

# local-only, not part of the GitHub submission repo
PS.odt
6a8ffdf33590a_round3-handout-updated.pdf
sample-marketplace/
WORKLOG.md

# pre-GitHub-architecture skills, superseded by the six under skills/ in marketplace.json.
# NOT crawl-access-audit/scripts/ -- that rule used to belong here too, but as of
# 2026-09-03 that path holds the current, needed crawl_access_checks.py and was silently
# excluding it from every commit. Removed.
brand-ai-readiness-audit/skills/engagement-onsite-audit/
brand-ai-readiness-audit/skills/entity-identity-corroboration/
brand-ai-readiness-audit/skills/structured-data-extraction/

# local evaluation harness (Phase 4) -- never part of the submission zip
harness/

# generated working artifacts handed to the user directly (e.g. the gold-labelling kit
# zip) -- reproducible from harness/ output, not part of the submission zip
dist/
